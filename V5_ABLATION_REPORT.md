# V5 ablation report

`V5_ABLATION_RESULTS.csv` separates A0 direct native execution through A6
profile-aware execution. The evidence supports these narrow statements:

- private selection alone does not hide later activation/control;
- hierarchical resolution adds a route decision whose privacy depends on the
  selected profile;
- Control Virtualization preserves 12/12 fresh bounded semantic cases and keeps
  logical Agent identity out of the common executor identity;
- the repaired fixed transcript/Gateway completes one development workflow
  under STANDARD/LONG with zero dummy heavy operations;
- full V5 trajectory and timing privacy remain open.

No standard PIR, ORAM, PSI, AEAD, or pacing primitive is claimed as novel.
