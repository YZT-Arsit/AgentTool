# Current native private-routing gate

The first fresh precheck is permanently preserved as 9/15 because the new common assertion read an OpenAI-only evidence key on Microsoft rows. Runtime projection and operation checks had already passed. No runtime source changed and those identities were not retried.

With a framework-neutral evidence assertion and fresh `DEV-V12-NTRC2-*` identities, the current `v11_online.frameworks` gate passed **15/15**. It covers repeated ordinary Tools, distinct operation IDs, OpenAI/Microsoft Agent-as-Tool, Tool→Agent-as-Tool, Agent-as-Tool→Tool, effect/outcome mapping, exact native invocation counts, and private-alias exclusion from semantic/provider-visible values.

Relay/structural/size alias absence is preserved from static dataflow and prior canonical evidence; it was not rerun through the timing-dependent online session.
