# V12 timing Microsoft depth-contract closure

The Microsoft native depth defect is closed at the component boundary, but the phase-level fresh live-capacity gate failed and the phase stops here.

The pinned Microsoft API exposes a supported public configuration path. Its iteration budget counts LLM roundtrips; a forced final tools-disabled response occurs outside the budget loop. The mathematically required public value for 50 sequential one-call turns is therefore 50. Linux native boundary tests passed `11/11`, including depth 41, depth 50 plus final response, and rejection of a 51st real operation by our `M=50` system boundary. The configuration is constant across identities, Tool patterns, counts, and Agent-as-Tool use.

The OpenAI adapter's private-count-derived `max_turns` was replaced by the fixed public value 52. Post-change Linux Python gates passed `62/62` serial and `62/62` default. Native routing remained `15/15`; Go evidence is dependency-preserved at `70/70`; non-timing security is `22/22` with the two affected negatives rerun.

Deployment integrity passed `696/696` runtime hashes, `10/10` import-file probes, and `2/2` binary hashes.

The one-shot fresh live campaign then failed on its first identity, `DEV-MDCC-OA-SAME-AGENT-DEPTH50-001`, before Microsoft live execution. Native OpenAI completed 50 operations, but canonical execution delivered 46/50; four descriptor-resolved intents occurred after the `H=3000 ms` admission horizon. The public transcript remained complete at 356/356 cells and PIR remained exactly 1 real + 99 dummy with 49 cache hits. The unisolated host recorded 236 schedule misses and a 30.622045 ms maximum launch slip. This is an integrated live-capacity failure, not a Microsoft iteration or PIR-query-count failure.

No retry, substitution, timing attack, confirmatory timing session, final candidate universe, seed, authorization, or selected V12 execution occurred. `TIMING_PRIVACY` remains `INCONCLUSIVE`, `TIMING_GO=NO`, and readiness for timing attack development and final holdout is `NO`.
