# V12 current-runtime reachability

This classification was frozen before the new current-runtime Python manifest and before decisive execution.

The future V12 entrypoint imports `v11a_confirmatory.orchestrator`, which dispatches native actions through `v11_full_scope.frameworks` and online trajectories through `v11_online.frameworks`. Canonical execution uses `v11_online.session.CanonicalOnlineSession`, the frozen prebuilt `acv-simplepir-online`, evidence providers, and the canonical Go runner/Gateway path.

Neither static references nor an import-only driver probe found `agent_control_virtualization.compiler_v2`, `runtime_v2`, `AgentRuntimeV2`, `compile_workload_v2`, `CALL_AGENT`, or `RETURN_AGENT` in the selected-runtime closure. Agent-IR V2 is therefore **LEGACY_COMPATIBILITY**. Its two historical Agent-as-Tool failures remain `FAIL 0/2`, but they are not a current private-routing gate.

Canonical online session tests are classified timing-dependent because the current implementation couples them to the unresolved hard-deadline public profile. Native framework routing, manifest parsing, provider classification, recovery, PIR process selection, codecs, authorization, projections, and integrity guards are non-timing current/runtime-support dependencies.
