# Action Semantic Holdout V7

The frozen pre-OHTTP holdout contains 24 source-traceable deterministic cases
across OpenAI Agents SDK and Microsoft Agent Framework action families. All
24/24 native and mediated projections matched, with zero dummy heavy
operations.

This validates adapter/action semantics and the reliable result substrate. It
does **not** validate RFC 9458, RFC 9292, OHTTP key handling, Relay opacity, or
encapsulated size equality. The CSV and its freeze hash are preserved unchanged.

Status: `FRESH_ACTION_SEMANTIC_FIDELITY = 24/24 PASS (PRE-OHTTP SEMANTICS)`.

