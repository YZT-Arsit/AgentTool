# Structural/size long-horizon V5 status

**NOT RUN: V5 functional gate incomplete.** The repaired development Gateway
completes one bounded Tool workflow under `STANDARD` and `LONG`, but the full V5
path has not completed all required Agent identity, STRICT route, handoff, Tool,
frequency, rare-target, transition, Agent-as-Tool, and cross-session workflows
through a hardware-attested boundary.

Accordingly no new holdout was frozen and no classifier was run. The old broken
AUC 0.500 artifact remains invalid as whole-workflow evidence. Machine-readable
rows in `STRUCTURAL_SIZE_LONG_HORIZON_V5.csv` are
`NOT_RUN_FUNCTIONAL_GATE_INCOMPLETE`.

Timing is also open/not tested by design. A new structural/size holdout may be
created only after every included workflow returns or reaches a predeclared
public failure class.
