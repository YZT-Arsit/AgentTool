# V12 security-negative audit

All 22 predeclared fail-closed rows were exercised by the referenced local Python/Go regression suites and passed; the retained Go JSON test stream records **92 pass events and zero fail events**. Matrix status: **PASS**. The final all-history local Python run itself was **299/302**, so the separate full-unit-test pre-holdout gate is **FAIL**. A no-change serial recheck passed the two bounded-timeout failures but reproduced `test_tool_multi_action_capacity[50]` with `canonical response final size mismatch`; neither result is discarded or promoted into the security-negative denominator. Provider effects and dummy-heavy work are zero for rejected-before-provider cases. Recovery ambiguity is explicitly `EFFECT_OUTCOME_UNKNOWN`, not an exactly-once claim. Timing privacy was not tested. A separate Linux all-history pytest attempt is preserved as environment-incomplete evidence because that checkout intentionally lacks several historical ignored result trees.
