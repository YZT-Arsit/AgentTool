# Current routing precheck failure — preserved

The first fresh native routing precheck produced **9/15 PASS and 6/15 FAIL**; two canonical timing nodes were predeclared deselections. All six failures were Microsoft rows and occurred after exact projection, logical action, argument, and operation-ID checks passed. The newly added common assertion then attempted to read the OpenAI-only `tool_output_count` key. Microsoft exposes the corresponding evidence as `observed_function_results`.

This is preserved as a new-test assertion schema failure, not rewritten as PASS. No runtime source changed. The corrected framework-neutral assertion uses fresh `DEV-V12-NTRC2-*` identities; the prior identities are not retried.
