# Scientific interpretation

The P10 collection event is a **semantic reliability abort during timing-development collection**. It is not timing-privacy evidence.

The original framework log proves that Microsoft recognized and invoked both routed functions. The ordinary anchor entered AgentTool and failed because an admitted result remained pending when the fixed public session ended. The subsequent Agent-as-Tool child entered AgentTool and failed because its descriptor cache miss arrived after the public PIR real-resolution cutoff. The empty adapter execution list represented zero *successfully returned* implementations, not zero function invocations.

A predeclared 1,200-identity semantic-only reliability matrix produced 200/200 exact successes in each of six coordinates, including actual T7 class 0/class 1 adapters, ordinary-only, Agent-as-Tool-only, unique routed names, and the current repeated routed names. This rules against a deterministic Microsoft parent-invocation defect, Agent-as-Tool registration defect, routed-name collision, or deterministic T7 workload-definition defect in the semantic path tested.

The available original diagnostics do not record enough resource state to distinguish a transient host/runtime stall from cumulative long-running resource pressure or another unrecorded cause of the fixed-session capacity miss. The frozen root-cause category is therefore `ROOT_CAUSE_UNRESOLVED`, with the proximate failure sequence established exactly.

No timing AUC, P10 timing privacy result, or sentinel PASS/FAIL may be reported. The original 1,203 complete sessions remain permanently excluded. A new sentinel is not authorized by this closure.
