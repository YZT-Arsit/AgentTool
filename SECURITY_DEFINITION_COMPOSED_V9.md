# Composed Registry + Relay Security Definition — V9

The optional colluding-observer game combines the Registry and Relay views. It
requires both official SimplePIR query privacy and the canonical RFC 9458/RFC
9292 channel/profile. SimplePIR is not required inside the Relay-only game, and
OHTTP is not credited for registry-index privacy.

Status: **OPEN / NOT INSTANTIATED END TO END**. Both component mechanisms now
execute, but no single canonical run generated a joined Registry+Relay view
through durable provider execution and trusted framework delivery. Component
success is not used to claim composed indistinguishability.

